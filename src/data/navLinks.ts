import type {NavLinks} from "../types.ts";

const navLinks: NavLinks[] = [
	{ label: "intro", href: "#hero" },
	{ label: "features", href: "#features" },
	{ label: "product", href: "#testimonies" },
	{ label: "contact", href: "#contact" },
]

export default navLinks;